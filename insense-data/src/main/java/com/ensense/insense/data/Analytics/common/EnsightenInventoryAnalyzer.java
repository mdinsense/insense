package com.ensense.insense.data.analytics.common;

public class EnsightenInventoryAnalyzer {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}
	
	
	/* 
	 * Detect if page is ensighten enabled
	 * if ensighten enabled, find the tags that are deployed via Ensighten
	 * if not Ensighten enabled (if uses lazy js), list out the tags firing on the page
	 * also check if the page does not have lazy or ensighten on it.
	 * 
	 * report
	 */

}
